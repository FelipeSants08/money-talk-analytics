spring:
  application:
    name: money-talk-analytics
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-3.6-flash
coingecko:
  api:
    key: ${API_KEY_COINGECKO}